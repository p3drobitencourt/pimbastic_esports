<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Liquidação Financeira - Pimbastic Esports</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Chakra+Petch:wght@400;600;700&family=Bebas+Neue&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-1: #081019; --bg-2: #132737;
            --accent: #10b981; --accent-2: #22d3ee;
            --text: #f4f7fb; --muted: #b9c2cf;
            --card: rgba(10, 20, 32, 0.7); --border: rgba(255, 255, 255, 0.14);
        }

        body {
            margin: 0; min-height: 100vh; color: var(--text);
            font-family: "Chakra Petch", sans-serif;
            background: linear-gradient(140deg, var(--bg-1), var(--bg-2));
            padding: 40px 20px;
        }

        .container { max-width: 700px; margin: 0 auto; }

        .panel {
            background: var(--card); border: 1px solid var(--border);
            border-radius: 18px; backdrop-filter: blur(10px);
            padding: 32px; box-shadow: 0 24px 60px rgba(0,0,0,0.35);
        }

        h1 { font-family: "Bebas Neue", sans-serif; margin: 0 0 8px; font-size: 36px; }
        .subtitle { color: var(--muted); font-size: 14px; margin: 0 0 24px; }

        .back-link { display: inline-block; color: var(--accent); text-decoration: none; font-size: 13px; margin-bottom: 16px; transition: color 200ms; }
        .back-link:hover { color: var(--accent-2); }

        .feedback-success { background: rgba(16, 185, 129, 0.12); border: 1px solid rgba(16, 185, 129, 0.45); color: #bbf7d0; padding: 14px 16px; border-radius: 12px; margin-bottom: 24px; }

        .game-resolution-card {
            background: rgba(255,255,255,0.03); border: 1px solid var(--border);
            border-radius: 12px; padding: 20px; margin-bottom: 20px;
        }

        .game-title { font-size: 18px; font-weight: bold; margin-bottom: 5px; color: var(--text); }
        .game-time { font-size: 12px; color: var(--muted); margin-bottom: 15px; }

        .badge-abertas { background: var(--accent); color: #051220; padding: 4px 8px; border-radius: 4px; font-size: 11px; font-weight: bold; margin-bottom: 15px; display: inline-block; }
        .badge-resolvido { background: rgba(255,255,255,0.1); color: var(--muted); padding: 4px 8px; border-radius: 4px; font-size: 11px; font-weight: bold; margin-bottom: 15px; display: inline-block; }

        label { font-size: 13px; color: var(--muted); display: block; margin-bottom: 8px; }
        
        select {
            width: 100%; border-radius: 10px; border: 1px solid rgba(255, 255, 255, 0.18);
            background: rgba(0, 0, 0, 0.3); color: var(--text); padding: 10px 12px;
            font-family: "Chakra Petch", sans-serif; margin-bottom: 15px;
        }
        select option { color: #0b1220; }

        button {
            width: 100%; border-radius: 10px; border: none; cursor: pointer;
            background: linear-gradient(120deg, var(--accent), var(--accent-2));
            color: #051220; font-weight: 700; padding: 12px; transition: transform 180ms ease;
        }
        button:hover { transform: translateY(-2px); }
        button:disabled { background: rgba(255,255,255,0.1); color: var(--muted); cursor: not-allowed; transform: none; }
    </style>
</head>
<body>
    <main class="container">
        <div class="panel">
            <a href="index.php" class="back-link">← Voltar para Dashboard</a>
            
            <h1>Liquidação Financeira</h1>
            <p class="subtitle">Encerre partidas para processar perdas e ganhos nas carteiras dos clientes automaticamente.</p>

            <?php if (isset($_GET['success'])): ?>
                <div class="feedback-success">
                    ✓ Partida liquidada com sucesso! Vencedores foram pagos e perdedores registrados.
                </div>
            <?php endif; ?>

            <?php foreach ($viewData['jogos'] as $jogo): ?>
                <div class="game-resolution-card">
                    <div class="game-title"><?= htmlspecialchars($jogo['campeonato']) ?>: <span style="color: var(--accent-2)"><?= htmlspecialchars($jogo['casa']) ?> vs <?= htmlspecialchars($jogo['fora']) ?></span></div>
                    <div class="game-time">Data do Jogo: <?= date('d/m/Y H:i', strtotime($jogo['data_horario'])) ?></div>
                    
                    <?php if ($jogo['apostas_abertas'] > 0): ?>
                        <div class="badge-abertas"><?= $jogo['apostas_abertas'] ?> APOSTA(S) PENDENTE(S)</div>
                    <?php else: ?>
                        <div class="badge-resolvido">NENHUMA APOSTA PENDENTE</div>
                    <?php endif; ?>

                    <form method="POST" action="resolver.php">
                        <input type="hidden" name="jogo_id" value="<?= $jogo['id'] ?>">
                        
                        <label>Declare o Resultado Oficial da Partida:</label>
                        <select name="resultado_real" <?= $jogo['apostas_abertas'] == 0 ? 'disabled' : 'required' ?>>
                            <option value="">Selecione o resultado final...</option>
                            <option value="vitoria_casa">Vitória: <?= htmlspecialchars($jogo['casa']) ?></option>
                            <option value="empate">Empate</option>
                            <option value="vitoria_fora">Vitória: <?= htmlspecialchars($jogo['fora']) ?></option>
                        </select>
                        
                        <button type="submit" <?= $jogo['apostas_abertas'] == 0 ? 'disabled' : '' ?>>
                            <?= $jogo['apostas_abertas'] > 0 ? 'Encerrar Partida e Distribuir Prêmios' : 'Partida Liquidada' ?>
                        </button>
                    </form>
                </div>
            <?php endforeach; ?>
        </div>
    </main>
</body>
</html>